This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

You can start editing the page by modifying `app/page.js`. The page auto-updates as you edit the file.

## INSTALLATION 

=> React router dom 
cmd : npm i react-router-dom
version: "react-router-dom": "^7.2.0",

=> React icons
cmd : npm i react-icons
version: "react-icons": "^5.5.0",

=> React inner zoom 
cmd : npm i react-inner-image-zoom
version: "react-inner-image-zoom": "^3.0.2",

=> React lazy load image component
cmd : npm i react-lazy-load-image-component
version: "react-lazy-load-image-component": "^1.6.3",

=> Swiper
cmd : npm i swiper
version: "swiper": "^11.2.4",

=> Zod
cmd : npm i zod
version: "zod": "^3.24.2"

=> React collapse
cmd : npm i react-collapse
version: "react-collapse": "^5.1.1",

=> Jose
cmd : npm i jose
version: "jose": "^6.0.8",

=> dotenv
cmd : npm i dotenv
version: "dotenv": "^16.4.7",

=> Axios
cmd : npm i axios
version: "axios": "^1.8.2",

=> Material UI
cmd : npm install @mui/material @emotion/react @emotion/styled
version:
   "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/material": "^6.4.6",

=> Tailwind Css
cmd => 
npm install -D tailwindcss@3 postcss autoprefixer
npx tailwindcss init
version : "postcss": "^8.5.3",
          "tailwindcss": "^3.4.17"


=> Install node_modules 
cmd : npm i


# env
set .env.local file at the root of the folder
=> create this variables in env.local file 
1.SESSION_SECRET=<>Your secret key </>

